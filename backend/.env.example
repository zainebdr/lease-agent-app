# Copy to .env and fill in only if you want to use a real LLM instead of
# the built-in mock. The app runs completely fine with none of these set.

# DATABASE_URL=postgresql://user:password@localhost:5432/lease_agent
# ANTHROPIC_API_KEY=sk-ant-...
